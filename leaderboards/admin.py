from django.contrib import admin

# Register your models here.
# Register your models here.
from .models import ConnectionsScore

admin.site.register(ConnectionsScore)
##added connection scores here so we can edit in the admin UI